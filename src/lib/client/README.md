# lib/client

Browser-only helpers. Safe to import from `"use client"` components.

Lives here:

- Supabase **anon** client (browser).
- Wallet adapter wiring (Phantom, Solflare, ...).
- UI hooks and local state.

Never put service-role keys or server secrets here.
